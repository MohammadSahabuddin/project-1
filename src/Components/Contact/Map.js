//Packages
import { Box, Typography } from '@mui/material';

const Map = () => {
  return (
    <Box>
      <Typography variant="p" component="p">
        Maps
      </Typography>
    </Box>
  );
};

export default Map;
