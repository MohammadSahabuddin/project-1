//Packages
import { Box, Typography } from '@mui/material';

const Title = () => {
  return (
    <Box>
      <Typography variant="h4" component="h4">
        RequestContent
      </Typography>
    </Box>
  );
};

export default Title;
